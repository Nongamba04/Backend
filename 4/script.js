// npm : Node Package Manager
// node_modules :stores every module an user requires for its project
// npm install - command will install the node modules folder according to the dependencies mentioned inside the package.json
// example : npm install slugify@1.3.5,to install new packages inside the dependencies (dependent packages )

// npm install nodemon --save-dev : devDepencies(Only uses in development and not in production)

// For global installation : npm install (Package name) --global

// npm i is also generally used as a install command
